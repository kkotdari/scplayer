node_modules/
dist/
.env.local
.env

# 타입 검사가 남기는 증분 캐시 — 산출물이라 안 담는다(`tsc`를 돌리면 생긴다).
*.tsbuildinfo

# npm이 남기는 잠금 파일 — 여기서는 **일부러 안 담는다**. scplay 의존성이 커밋을 안
# 박고 `github:kkotdari/scplay`로 걸려 있어, 빌드할 때마다 그쪽 main의 최신 커밋을
# 가져가는 것이 이 저장소의 배포 방식이다. 잠금 파일을 담으면 그 자리 커밋이 얼어붙어
# scplay를 배포해도 여기가 안 따라온다.
